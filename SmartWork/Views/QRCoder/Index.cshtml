@model Byte[]
@{
    ViewBag.Title = "User QR code";
}

<div class="container">
    <div id="content">
        <h1>QR Code</h1>

        @{
            if (Model != null)
            {
                <h3>QR Code Successfully Generated</h3>
                <img src="@String.Format("data:image/png;base64,{0}", Convert.ToBase64String(Model))" />
            }
        }
    </div>
</div>
